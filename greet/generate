protoc greet/greetpb/greet.proto --go_out=.

//need to add "-grpc", it adds the plugins
protoc greet/greetpb/greet.proto --go-grpc_out=.

// to add both the grpc module and the standard proto one
protoc greet/greetpb/greet.proto --go-grpc_out=. --go_out=.